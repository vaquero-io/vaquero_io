platform:
  product: <% %>
  provider: <% %>
  plugin_version: <% %>

  environments:
    -

  nodename:
    - environment
    - component

  require:
    <% %>

  profiles:

    defaultprofile: &defaultprofile
      <% %>

  roles:

    example:
      <<: *defaultprofile